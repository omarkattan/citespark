Spend panel layout fix - 3 files, drag src/ and scripts/ to the repo root.

  src/public/app.js     spend rows use their own class names, not one scoped
                        to another component
  src/public/styles.css every layout property stated; names own the space,
                        rates and amounts never wrap
  scripts/test.js       pins it

Verify after deploy, in a FRESH shell (then hard-refresh the browser,
styles.css is cached):
  grep -c "spend-name" src/public/styles.css    -> 1 or more

Question-row action bar - 4 files, drag src/ and scripts/ to the repo root.

  src/public/app.js     actions grouped into a bar; "Copy article brief"
                        carries the accent on questions you are losing
  src/public/styles.css the bar, compact buttons, the accent, select set apart
  src/server.js         feature marker only
  scripts/test.js       pins it (270 -> 271 checks)

Verify after deploy, in a FRESH shell:
  grep -c "q-actions" src/public/app.js         -> 2 or more
  grep -c "q-cta" src/public/styles.css         -> 2 or more

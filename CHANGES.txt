Demand table row types - 4 files.

src/lib/demand.js   unmatched clusters report null, not 0; measurable flag;
                    gap threshold named; sort puts gaps first, unmatchable last
src/public/app.js   unmatched rows read "no matching queries" and the note
                    explains why their demand is unknown, not zero
scripts/demand.js   same distinction in the shell table
scripts/test.js     pins it

No migration.

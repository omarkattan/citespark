Feedback fixes 1 and 2 - 4 files.

src/lib/ga4.js     NEW ga4Readiness(project): one rule for whether Analytics
                   is usable, honouring the deployment credential, with a
                   distinct reason for each unready state
src/lib/report.js  the report now uses that rule. It tested the project own
                   token alone, so a site syncing fine through the shared
                   credential was told Analytics was not connected
src/public/app.js  "Not run - press run cycle" becomes "Not measured yet"
                   with a button that opens the run menu and scrolls to it
scripts/test.js    pins both (286 -> 288 checks)

Four pre-existing test failures are unchanged by this delta (verified against
the untouched upload).

No migration.

Search demand in the Traffic tab - 5 files.

src/lib/demand.js   NEW shared rule: per-cluster impressions/clicks beside
                    the AI named rate, gaps first, method text in one place.
                    Used by the script, the tab and (next) the PDF report.
src/server.js       GET /api/projects/:id/demand; queries carry a source so
                    Bing joins the same shape when connected
src/public/app.js   Traffic tab renders the table under the figures
scripts/demand.js   refactored onto the shared library
scripts/test.js     pins one-rule-three-surfaces

No migration. Verify: Traffic tab shows "Search demand against AI visibility".

GSC demand-vs-visibility script - 3 files (package.json goes to repo root).

scripts/demand.js  npm run demand -- 4 : per-cluster GSC impressions and
                   clicks beside the AI named rate, GAP lines flagged.
                   Read-only, one free GSC call, no schema.
scripts/test.js    pins the rules (never summed, literal match, absent!=zero)
package.json       adds the demand script

Verify: npm run demand -- 4

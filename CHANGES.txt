Cost card clarity - 2 files.

  src/public/app.js     header card renamed "Last cycle cost" (it is history
                        and does not move); its sub-line now shows the live
                        next-cycle estimate while the spend panel is used
  scripts/test.js       pins it

Verify after deploy: open the spend panel, untick an engine, watch the
header card's sub-line change to "Aug 29 - next about $X" while $21.10 stays.

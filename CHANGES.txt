Read-the-answers lands on the answers - 2 files.

src/public/app.js  scrolls to the question, opens its answers, highlights
                   the row; a filter hiding it is named instead of silence
scripts/test.js    pins it

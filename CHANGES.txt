Spend panel, layout carried inline - 2 files.

  src/public/app.js     row and column layout now travels inline with the
                        generated markup; no stylesheet can break it and no
                        stylesheet cache can withhold it
  scripts/test.js       assertions follow the layout to its new home

Only app.js matters at runtime, and app.js is what the browser refetches on
a normal reload far more reliably than styles.css.

Verify after deploy, in a FRESH shell:
  grep -c "const ROW = 'display:flex" src/public/app.js   -> 1

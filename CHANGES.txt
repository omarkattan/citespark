Brief shown inline - 2 files.

src/public/app.js  CTA renamed See suggested content; opens the brief in an
                   inline panel under the question with Copy and Close;
                   blocked clipboard selects the text instead of a new tab
scripts/test.js    pins it

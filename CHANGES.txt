Connect-in-Setup button - 2 files.

src/public/app.js  the not-connected demand panel now offers a real button
                   (reuses the existing data-goto-setup handler) instead of
                   telling the reader to go somewhere
scripts/test.js    pins it

No migration.

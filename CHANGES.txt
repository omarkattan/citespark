Reask never fails silently - 2 files.

src/public/app.js  raw fetch with every exit handled: button always resets,
                   server errors and never-completed requests are named
scripts/test.js    pins it

Verify: Ask again now either shows the outcome toast or a toast naming
exactly why not. A frozen button is no longer a possible state.

Cost transparency - 4 files, drag src/ and scripts/ to the repo root.

  src/server.js         GET /api/projects/:id/spend (breakdown by engine and
                        intent, cost beside named rate) and
                        PATCH /api/projects/:id/intents (pause a whole intent)
  src/public/app.js     the Cycle cost figure now opens the breakdown; every
                        row has a switch; live next-cycle projection vs cap
  src/public/styles.css the panel and the clickable figure
  scripts/test.js       pins it (271 -> 272 checks)

Verify after deploy, in a FRESH shell:
  grep -c "data-spend" src/public/app.js         -> 3 or more
  grep -c "/spend'" src/server.js                -> 1

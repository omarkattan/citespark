Article brief feature - 4 files, drag src/ and scripts/ to the repo root.
Files not in this upload are untouched by GitHub's uploader.

  src/lib/brief.js      NEW  the brief template, filled from measured evidence
  src/server.js         GET /api/prompts/:id/brief route (+ feature marker)
  src/public/app.js     "Copy article brief" button on each question
  scripts/test.js       pins the behaviour (269 -> 270 checks)

Verify after deploy, in a FRESH shell:
  grep -c "data-brief" src/public/app.js        -> 2 or more
  grep -c "buildBrief" src/server.js            -> 1 or more

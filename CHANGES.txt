Portal labels and help bubbles - 3 files.

src/public/app.js   helpDot() promoted to shared use; "?" bubbles on the
                    traffic figures, all four demand columns, the panel
                    heading and Position among tracked brands. Labels
                    rewritten: "Visits from AI assistants", "Conversions
                    from those visits" (with denominator), revenue dash
                    explained. Each demand row shows how many queries it sums.
src/lib/demand.js   method text states the summing rule and says topic
                    instead of cluster
scripts/test.js     pins coverage and the denominators

No migration.

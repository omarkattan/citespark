Ask-again overlay - 2 files.

src/public/app.js  modal overlay: progress while asking, per-engine verdict
                   after, stays until closed, "Read the full answers" opens
                   the evidence directly. Errors land in the same modal.
scripts/test.js    pins it

Verify: Ask again now -> overlay with progress -> per-engine named/not table
-> Read the full answers opens the panel.
